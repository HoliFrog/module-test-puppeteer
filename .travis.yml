language: node_js

node_js:
  - 8

script:
  - ./node_modules/jest/bin/jest.js -v
  - npm test -- --runInBand

notifications:
  email: true

 env:
  global:
    secure: ... encoded secret ...
before_install:
  - echo $PASSWORD | gpg --passphrase-fd 0 credentials.json.gpg